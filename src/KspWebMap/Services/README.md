This folder is reserved for long-running services owned by the core lifecycle host.

Future examples include the localhost web server and any background workers that do not directly call Unity or KSP APIs from worker threads.
