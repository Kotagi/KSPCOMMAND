/** V2 re-exports canonical Map V3 context (telemetry → bodies / hierarchy). */
export {
  buildMapContext,
  isMoonBody,
  isPlanetBody,
  starBody,
  type BodyEntry,
  type MapContext,
} from "../map-v3/MapContext";
