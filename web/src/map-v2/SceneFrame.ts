/** V2 re-exports canonical Map V3 scene frame (root → scene coordinates). */
export {
  bodyEntriesForFocus,
  createSceneFrame,
  getFocusPositionV2,
  resolveSceneFrameFocus,
  toScenePoint,
  toScenePoints,
  type SceneFrameFocusMode,
  type SceneFrameState,
  type ScenePoint3,
} from "../map-v3/SceneFrame";
